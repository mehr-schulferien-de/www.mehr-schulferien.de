defmodule MehrSchulferienWeb.RobotsHTML do
  use Phoenix.View,
    root: "lib/mehr_schulferien_web/templates",
    path: "robots"

  # Robots controller doesn't use templates - sends plain text directly
end
