defmodule MehrSchulferienWeb.RedirectHTML do
  use Phoenix.View,
    root: "lib/mehr_schulferien_web/templates",
    path: "redirect"

  # Redirect controller doesn't use templates - only performs redirects
end
