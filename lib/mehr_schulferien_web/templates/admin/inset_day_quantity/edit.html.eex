<h2>Edit Inset day quantity</h2>

<%= render "form.html", Map.put(assigns, :action, admin_inset_day_quantity_path(@conn, :update, @inset_day_quantity)) %>

<span><%= link "Back", to: admin_inset_day_quantity_path(@conn, :index) %></span>
